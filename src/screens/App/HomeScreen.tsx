import { Typography } from "@mui/material";

export const HomeScreen: React.FC = () => {
    const title = <Typography variant='h4'>GART-WEB</Typography>;

    return (
        <>
            {title}
        </>
    );
};