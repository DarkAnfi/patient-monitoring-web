const patients = {
    createPatient: '[patients] Create patient',
    updatePatient: '[patients] Update patient',
    deletePatient: '[patients] Delete patient',

    createPatientEvent: '[patients] Create patient event',
    updatePatientEvent: '[patients] Update patient event',
    deletePatientEvent: '[patients] Delete patient event',
};

export default patients;